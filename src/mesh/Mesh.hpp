#pragma once

#include <vector>
#include "glm/glm.hpp"

namespace Levek {
class Mesh {
    std::vector<glm::vec3> vertices;
    std::vector<glm::vec2> textures;
    std::vector<glm::vec3> normals;
private:

public:


};
}